# AWS PoCs

## Setup
```bash
aws configure sso   # or: aws configure --profile <profile>
export AWS_PROFILE=<profile>
```
- Default region: `<region>`
- Bedrock models need model access to be enabled per region.

## PoCs
| PoC | Status | Summary |
| --- | ------ | ------- |
