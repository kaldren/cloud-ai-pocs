# GCP PoCs

## Setup
```bash
gcloud auth login
gcloud auth application-default login
gcloud config set project <project-id>
```
- Default region: `<region>`
- Consider a separate project for PoCs so billing and cleanup stay isolated.

## PoCs
| PoC | Status | Summary |
| --- | ------ | ------- |
